# campus_assistand backend setup

python db_reset.py
uvicorn app.main:app --host 0.0.0.0 --port 8000


Wait for server → Ctrl+C → then:

python db_setup.py
uvicorn app.main:app --host 0.0.0.0 --port 8000

flutter run